# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# ต้นฉบับความละเอียดสูงสำหรับสร้าง favicon (assets/iconweb.png ~5MB)
# ไม่ต้องเข้า git — ผลลัพธ์ที่เว็บใช้จริงคือ app/icon.png ซึ่งถูก commit ไว้แล้ว
# ถ้าต้องสร้างไอคอนใหม่ ให้วางต้นฉบับกลับมาแล้วรัน node scripts/make-favicon.mjs
/assets

# typescript
*.tsbuildinfo
next-env.d.ts
